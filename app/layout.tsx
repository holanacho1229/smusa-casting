import type { Metadata } from "next";
import { Syne, Inter } from "next/font/google";
import "./globals.css";

const syne = Syne({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  variable: "--font-syne",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "The Documentary Project — Scalp Micro USA",
  description:
    "Apply for a chance to receive a fully sponsored SMP treatment with Matt Iulo. Share your hair loss story and transform your life on camera.",
  openGraph: {
    title: "The Documentary Project — Scalp Micro USA",
    description:
      "Apply for a chance to receive a fully sponsored SMP treatment. Share your story. Transform your life.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${syne.variable} ${inter.variable} h-full`}
    >
      <body className="min-h-full flex flex-col antialiased">{children}</body>
    </html>
  );
}
